# trademark-check allowlist: PATH-GLOB<TAB>PHRASE-REGEX<TAB>reason (one line each)
# Only the lines our patches ADD are scanned (keyed PATCH:TARGET-FILE); Wine's own strings are upstream's.
*:programs/notepad/syntax.c	^Windows Registry Editor$	recognises the .reg file format by its header line
*:programs/notepad/*	Windows \(CRLF\)	names the Windows line-ending convention, as a text format (the status bar and Save as)
*:loader/wine.inf.in	^Microsoft Windows Network$	the network provider's name, which programs look up (WNetGetProviderName); an identifier
*:programs/explorer/shellkeys.c	^Windows Power&Shell$	names Microsoft's own program when the user has it (Win+X's fallback)
*:programs/winecfg/appdefaults.c	^Windows 10$	the Windows version reported to programs (a compatibility setting)
# The English resources of the Wine programs and dialogs a user can reach
# (a series-applied tree, scanned with --root): Wine's own text ships in our OS too.
programs/cmd/cmd.rc	^Microsoft Windows %1!S!\\n$	cmd's banner and `ver` output, which batch scripts parse for the version (compatibility)
programs/notepad/notepad.rc	Windows \(CRLF\)	names the Windows line-ending convention, as a text format
programs/winetest/winetest.rc	the Windows API	names the API the tests check (technical); winetest is a developer tool
