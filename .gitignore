/build/

# debhelper output from `make deb`. Regenerated on every image build, so
# without this it sits untracked in the tree waiting to be committed by accident.
/debian/.debhelper/
/debian/debhelper-build-stamp
/debian/files
/debian/*.substvars
/debian/*.debhelper.log
/debian/wine-sg/
