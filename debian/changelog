wine-sg (10.0-64) unstable; urgency=medium

  * patches/sg/0241 user32: ExitWindowsEx shuts down and restarts the PC:
    after wineboot --end-session it starts sg-settingsctl shutdown
    poweroff|reboot (sg-session 0.1.0-34), which waits for the session's
    programs and asks logind. Start's Shut down and Restart did nothing.
  * patches/sg/0242 wineboot, server: signed-in users' programs read the
    display configuration back (HKLM\HARDWARE\DEVICEMAP\VIDEO and the
    hardware profile's Control\Video, created by wineboot as SYSTEM and
    user-writable): no more "Failed to read display config", Settings'
    "0 x 0" resolution, or Media Player crashing on video.
  * Gates: test/shutdown-gate.sh, test/dispcfg-gate.sh. Both found by the
    2026-09-26 ISO QA pass.

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 20:57:41 -0700

wine-sg (10.0-63) unstable; urgency=medium

  * Trademark hygiene in the text our patches add: calc/mspaint/... hand-off
    says "Stained Glass cannot find" (0120); file types are "Batch File",
    "Command Script", "PowerShell Script" (0152, new prefixes).
    tools/trademark-check.py scans the added lines of patches/sg in
    make lint (a CI step); tools/trademark-allow.txt lists the exceptions.

 -- Stained Glass OS <dev@stained-glass.example>  Fri, 25 Sep 2026 19:48:36 -0700

wine-sg (10.0-62) unstable; urgency=medium

  * patches/sg/0178 kernelbase: HKEY_CLASSES_ROOT is the merged view --
    HKCU\Software\Classes over HKLM\Software\Classes, for keys, values,
    enumeration and counts; new keys go to the machine's classes, or the
    user's when that is denied. 0179 shell32: the user's UserChoice
    (FileExts and UrlAssociations) decides the program for
    AssocQueryString, IQueryAssociations, ShellExecute and
    QueryCurrentDefault. Gate: make test-hkcr (two users on one prefix).
  * patches/sg/0220 win32u, usp10: CJK Extension B characters fall back to
    SimSun-ExtB, HanaMinB or BabelStone Han (sg-image ships
    fonts-hanazono). Gate: make test-astral.
  * patches/sg/0221 kernelbase: SetTimeZoneInformation,
    SetDynamicTimeZoneInformation, SetLocalTime and SetSystemTime go
    through sg-admind (timezone, time) when the process may not set them
    itself. Gate: make test-tzset.
  * patches/sg/0222 dwrite: a glyph is hinted at the size it is drawn at
    (cairo/GTK draw at em size 1 with the size in the transform) --
    Pinta's misshapen text. Gate: make test-dwscale.
  * patches/sg/0223 d2d1: all built-in effects, with properties; .NET
    ComWrappers effect factories; ID2D1EffectContext2; drawing keeps the
    device's feature level; WIC/DC/HWND targets at 11_1; Flush's drawing
    state; command lists; color contexts; ID2D1GradientStopCollection1;
    geometry realizations. 0224 dxgi: EnumWarpAdapter. 0225 d3dcompiler:
    GetMinFeatureLevel, GetRequiresFlags. 0226 uianimation: Windows
    Animation animates. 0227 d2d1: effect bounds, CombineWithGeometry,
    Widen. 0228 dxgi: CheckHardwareCompositionSupport. Paint.NET 5 gets
    through Direct2D to its compositor (Windows.UI.Composition, not in
    Wine). Gate: make test-d2dfx.

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 19:40:29 -0700

wine-sg (10.0-61) unstable; urgency=medium

  * patches/sg/0211 server, win32u: a domain account's token keeps this
    machine's Users group as owner (its primary group stays the domain's
    Domain Users), so display keys its session rewrites in HKLM stay
    deletable by the greeter and other users; win32u's reg_empty_key()
    stops at a key it cannot delete instead of retrying it for ever. The
    greeter spun at full CPU after the first domain user signed out.
    Gate: make test-domainsid (+3 checks; without the server change the
    key owner check fails) and sg-image's make domain-test (dave signs in
    after alice).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 18:46:08 -0700

wine-sg (10.0-60) unstable; urgency=medium

  * patches/sg/0240 riched20: tables survive a round trip through RTF (a
    row's first cell kept its \intbl; \trowd after \pard; \cell applies
    the cell's paragraph format) and are drawn as one grid (a row's start
    and end paragraphs have no spacing). Found by sg-shell's WordPad
    tables; gate: sg-shell test/wordpad-check.sh.

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 17:53:22 -0700

wine-sg (10.0-59) unstable; urgency=medium

  * patches/sg/0244 shell32: thumbnails of videos (a frame through Media
    Foundation: MP4, WebM, AVI, MKV, WMV, MOV, ...) and PDFs (the first
    page, rendered by sg-session's sg-pdf --thumbnail). Existing prefixes
    need `wineboot -u` for the new ShellEx keys.
  * patches/sg/0245 shell32: an on-disk thumbnail cache under
    %LOCALAPPDATA%\Microsoft\Windows\Explorer\sg-thumbcache, keyed by path,
    size, time and file size, bounded (ThumbnailCacheKB, default 100 MB).
  * patches/sg/0246 explorer: the details pane shows a picture's dimensions
    and bit depth and a video's length and frame size; "Remove from Quick
    access" for frequent folders; pinned folders reorder by dragging.
  * Gate: test/explorer3-gate.sh (make test-explorer3).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 17:34:52 -0700

wine-sg (10.0-58) unstable; urgency=medium

  * patches/sg/0250 winex11: German ß ü ö ä ´, French é è à ... have their
    layout's scan codes and virtual keys in a UTF-8 locale; the keyboard
    tables are built again when the X layout changes (a German Z typed Y in
    a program that got no MappingNotify).
  * patches/sg/0251 winex11: Ctrl+Alt is AltGr in ToUnicodeEx (German @ on
    Q, French # on 3), as on Windows.
  * Gate: test/kbdlayout-gate.sh (make test-kbdlayout).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 17:23:48 -0700

wine-sg (10.0-57) unstable; urgency=medium

  * patches/sg/0170: DwmFlush waits for the next vertical blank. Firefox's
    vsync thread spun on it and flooded its GPU process with IPC: pages did
    not load, the browser grew to 20+ GB and never shut down. Firefox now
    loads a page with its GPU process and exits in about a second.
  * patches/sg/0171: characters beyond the BMP (emoji, maths, CJK
    extensions) in GDI text -- one glyph per surrogate pair, fallback to
    the installed emoji/symbol fonts (Symbola in the image), Uniscribe's
    Surrogates script gets a fallback font (edit controls).
  * patches/sg/0172: SystemTimeToTzSpecificLocalTimeEx and
    TzSpecificLocalTimeToSystemTimeEx with the zone's dynamic rules;
    SetDynamicTimeZoneInformation exported.
  * patches/sg/0173: IDXGIOutput::WaitForVBlank waits for the next vblank.
  * patches/sg/0174-0176: Windows.System.DispatcherQueue; Windows 10 22H2
    (build 19045); DXGIDeclareAdapterRemovalSupport.
  * patches/sg/0177: DirectWrite's CreateFontFromLOGFONT finds the font GDI
    uses for a substituted name (Segoe UI, MS Shell Dlg).
  * Gates: test-vsync, test-firefox, test-astral, test-tzex,
    test-dispatcherq, test-winver, test-dwlogfont. Compat suite: nine more
    applications, all launching.

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 16:15:51 -0700

wine-sg (10.0-56) unstable; urgency=medium

  * patches/sg/0209: wintrust -- an RFC 3161 timestamp dates the signature
    (Store signers are valid for days), when the timestamp is of this
    signature, verifies and comes from a trusted time-stamping authority.
  * patches/sg/0210: bundles whose packages are signed on their own (the
    Store's): unmapped payload packages must be named by the manifest,
    signed, trusted and the bundle's; stub packages and CodeIntegrity.cat.
  * Gate: test/msix-gate.sh gains timestamps and self-signed payloads.

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 16:12:24 -0700

wine-sg (10.0-55) unstable; urgency=medium

  * patches/sg/0100 notepad: a minimap (View > Minimap, on for code files):
    the whole document in miniature right of the text, coloured by the
    highlighter, the visible lines shaded; a click centres the editor on
    the line under it, dragging follows; long files are compressed to the
    strip and never lexed past the view; dark mode follows the theme.
  * Gate: test/notepad-minimap-gate.sh (make test-notepad-minimap).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 16:09:07 -0700

wine-sg (10.0-54) unstable; urgency=medium

  * Dark mode, system-wide and live (patches/sg/0160-0163): light.msstyles
    gains a generated Dark colour scheme (theme/dark.py, run by build.sh);
    uxtheme's RefreshImmersiveColorPolicyState follows AppsUseLightTheme and
    every running program follows the switch (theme and system colours);
    DWMWA_USE_IMMERSIVE_DARK_MODE / CAPTION_COLOR / TEXT_COLOR title bars;
    the taskbar follows SystemUsesLightTheme. Gate: make test-darkmode.
  * The taskbar honours Settings > Personalization > Taskbar (0164): edge,
    auto-hide, small buttons, combining, centring, search, Task View;
    SHAppBarMessage state/position/SETSTATE. Gate: make test-taskbar.

 -- Stained Glass OS <dev@stained-glass.example>  Fri, 25 Sep 2026 15:58:26 -0700

wine-sg (10.0-53) unstable; urgency=medium

  * patches/sg/0150-0157: File Explorer, round 2.
    - comctl32: list view groups (LVM_ENABLEGROUPVIEW, LVM_INSERTGROUP and
      the rest; headers, collapsible groups) and the tile view; a selected
      icon is drawn as custom draw says, so File Explorer's icons are no
      longer tinted purple.
    - shell32: type names as Windows shows them ("Text Document", "PNG
      File", "File folder", "Application", "Compressed (zipped) Folder");
      image thumbnails (a WIC IThumbnailProvider of our own, EXIF
      orientation, IExtractImage handlers too, IShellItemImageFactory); the
      folder view's Medium, Large and Extra large icons with thumbnails,
      Tiles, Content and Group by; files dropped on a folder are copied or
      moved into it (the drop target was a stub); Send to (the SendTo
      folder, Desktop, Documents, removable drives, a type's sendto
      command) and Pin to Quick access; recent documents in Unicode.
    - explorer: every layout in View, Group by, the details (Alt+Shift+P)
      and preview (Alt+P) panes, Quick access (pinned and frequent folders,
      recent files; its page and its navigation pane branch), drops on the
      navigation pane.
  * Gate: test/explorer2-gate.sh (make test-explorer2).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 14:50:00 -0700

wine-sg (10.0-52) unstable; urgency=medium

  * patches/sg/0190-0191: WebView2 apps draw. With the user-installed
    Evergreen runtime, pages loaded and scripts ran but nothing was ever
    painted: WebView2's GPU process needs DCompositionCreateDevice and
    IDXGIFactory2::CreateSwapChainForComposition, both stubs. 0190
    implements composition swap chains for Direct3D 11 devices (presented
    into the target window with GDI, redrawn while the host paints over
    them); 0191 the version-1 DirectComposition device (window targets,
    visuals, Commit) and fixes IDCompositionVisual's vtable order in
    include/dcomp.idl.
  * Gate: test/webview2-gate.sh (make test-webview2; the runtime and the
    WebView2 SDK header are fetched at test time, never shipped).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 14:00:49 -0700
wine-sg (10.0-51) unstable; urgency=medium

  * patches/sg/0180: Wine's wordpad.exe (which write.exe and the rtffile and
    wrifile associations start) hands off to App Paths' wordpad.exe --
    sg-shell's WordPad.
  * patches/sg/0184 riched20: EM_FORMATRANGE (printing and print preview
    through RichEdit; Windows' return values and rc.bottom, its winetests
    pass); \pngblip/\jpegblip pictures read; \dibitmap pictures' colours
    (bits read 4 bytes too far); bitmap pictures written back on save; the
    RTF writer's \li/\fi; zoomed text no longer overlaps (a style's script
    cache follows its font height).
  * patches/sg/0188 win32u: a menu bar is painted with its MIM_BACKGROUND
    brush.
  * patches/sg/0100 notepad: a dark menu bar in the dark theme; Page
    Setup's Header and Footer boxes, with &l/&c/&r alignment;
    NOTEPAD_PRINT_EMF for tests.
  * Gate: test/notepad-print-gate.sh (make test-notepad-print).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 13:37:43 -0700

wine-sg (10.0-50) unstable; urgency=medium

  * patches/sg/0207: winex11 -- a program on another X display than the
    desktop's owner no longer dies on an X BadWindow at its first cursor
    clip: it checks the desktop's clip window on its own display and makes
    its own when it is not there.
  * patches/sg/0208: winex11 -- XRender drawing on the desktop (a virtual
    desktop's window) is clipped by the windows on it, as X11 GC drawing
    already was: a new wallpaper no longer covers every window.
  * Gate: test/display-gate.sh (make test-display).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 13:32:55 -0700

wine-sg (10.0-49) unstable; urgency=medium

  * patches/sg/0183: fontview.exe in system32/syswow64 (a launcher for App
    Paths' fontview.exe, sg-shell's font viewer); .ttf/.otf/.ttc/.fon open
    in it, with Install and Install for all users verbs; win32u loads a
    user's own fonts from HKCU\Software\Microsoft\Windows NT\CurrentVersion\
    Fonts (Windows 10's per-user fonts); explorer sends shell:fonts and
    %WINDIR%\Fonts to the Fonts view, opens shell:<known folder> itself, and
    wine.inf registers the shell: URL scheme (the Run box's shell:fonts,
    shell:downloads ... work).
  * Gate: test/fonts-gate.sh (make test-fonts).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 12:55:00 -0700

wine-sg (10.0-48) unstable; urgency=medium

  * patches/sg/0205: a domain account's real SID. sg-session records the
    account's SID, groups and names from winbind at sign-in
    (/run/stained-glass/domain-sids/<uid>, root's); the server gives the user
    that SID (tokens, files, HKCU) and their domain groups, and names domain
    accounts and groups for LookupAccountSid/Name and GetUserNameEx -- with
    no protocol change.
  * patches/sg/0206: cmd's `dir \\server\share` lists the share, not Z:.
  * Gates: test/domainsid-gate.sh (make test-domainsid), test/uncdir-gate.sh
    (make test-uncdir).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 12:24:57 -0700

wine-sg (10.0-47) unstable; urgency=medium

  * patches/sg/0181: magnify.exe and osk.exe in system32/syswow64 start
    App Paths' Magnifier and On-Screen Keyboard (sg-shell); explorer's
    Win+Plus / Win+Minus / Win+Esc drive Magnifier and Win+Ctrl+O turns
    the On-Screen Keyboard on and off.
  * patches/sg/0182: an appbar's space comes off the work area (the docked
    Magnifier and keyboard), given back when it goes; SPI_GETWORKAREA is no
    longer cached for the life of a process.
  * Gate: test/a11y-gate.sh (make test-a11y).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 11:45:39 -0700

wine-sg (10.0-46) unstable; urgency=medium

  * patches/sg/0200-0204: MSIX beyond one package, and winget installs MSIX.
    Windows.Foundation.Uri takes a DOS path (winget's downloaded MSIX); the
    manifest's dependencies and resources; frameworks deployed with a
    package, required, recorded and protected from removal; updates replace
    older versions, downgrades are refused; a bundle's resource packages for
    the user's language; app execution aliases (WindowsApps, on the user's
    PATH; CreateProcess and cmd follow them); IPackageManager6
    (RequestAddPackageAsync), IPackage4; http(s) package locations; a
    packaged process loads DLLs from its frameworks (ntdll's package graph)
    and GetCurrentPackageInfo lists them.
  * Gate: test/msix-gate.sh (make test-msix), with test/mkmsix.py writing
    packages and bundles; WINGET_DIR adds winget install/list/uninstall of
    an MSIX from a local server.

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 11:41:27 -0700

wine-sg (10.0-45) unstable; urgency=medium

  * patches/sg/0185: a service's own security descriptor -- kept in
    Services\<name>\Security as Windows keeps it and used by the SCM's
    access checks; Query/SetServiceObjectSecurity, sc sdshow and sc sdset.
  * patches/sg/0186: lusrmgr.msc and fsmgmt.msc; msinfo32 /report and /nfo
    wait until the report is written.
  * patches/sg/0187: audit events from the Linux side (logon, logoff,
    elevation) reach the Security log through an audit spool the Event Log
    service imports; wevtsvc.dll words them.
  * Gates: test/scm-sd-gate.sh (make test-scm-sd), test/audit-gate.sh
    (make test-audit), test/admintools-gate.sh extended.

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 11:31:34 -0700

wine-sg (10.0-44) unstable; urgency=medium

  * patches/sg/0168: the user's regional format is their choice -- a
    LocaleName in Control Panel\International (Settings > Region, the
    first-run setup) decides it instead of being rewritten to the Unix
    locale at every process start; a country chosen apart from it is kept.
  * Gate: test/region-gate.sh (make test-region).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 11:28:30 -0700

wine-sg (10.0-43) unstable; urgency=medium

  * patches/sg/0110: File Explorer -- command bar, breadcrumb address bar,
    search, navigation pane (Quick access, This PC, Network), status bar,
    This PC with capacity bars, /select, Windows' keys; opens This PC.
  * patches/sg/0111: the folder view's columns (Name, Date modified, Type,
    Size), sorting by value with arrows, This PC / Recycle Bin / Network /
    Local Disk names, light selection, rename selects the base name.
  * patches/sg/0112: copy/move/delete progress window and "Replace or Skip
    Files" (replace, skip, keep both); CopyFileEx/CopyFile2 report progress
    and can be cancelled.
  * Gate: test/explorer-gate.sh (make test-explorer).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 09:23:20 -0700

wine-sg (10.0-42) unstable; urgency=medium

  * patches/sg/0131: a program started on a pseudo console gets the
    console's standard handles (was none: cmd and PowerShell wrote nowhere
    under a terminal), and ResizePseudoConsole resizes (was E_NOTIMPL).
  * patches/sg/0132: conhost interprets the VT sequences programs write
    with ENABLE_VIRTUAL_TERMINAL_PROCESSING (PowerShell 7's colours and
    line editing).
  * patches/sg/0133: wt.exe in system32 hands off to the App Paths terminal
    (sg-shell's Terminal); Win+X has Terminal and Terminal (Admin).
  * Gates: sg-shell's test/terminal-check.sh; test/shellkeys-gate.sh (Win+X
    Terminal).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 08:52:06 -0700

wine-sg (10.0-41) unstable; urgency=medium

  * patches/sg/0130: Win+I, and the Win+X menu's Settings, Apps and
    Features and System, open Settings through ms-settings: URIs
    (sg-shell's sg-settings); the Control Panel where no handler is
    registered. Gate: test/shellkeys-gate.sh (Win+I), sg-shell's
    test/settings-check.sh WINI=1.

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 07:57:21 -0700

wine-sg (10.0-40) unstable; urgency=medium

  * patches/sg/0141: the Service Control Manager checks who is asking --
    standard users may enumerate and query services but not start, stop,
    pause, reconfigure, delete or create them (Wine's own system32 services
    may still be started on demand). Gate: test/scm-access-gate.sh.
  * patches/sg/0142: mmc.exe, eventvwr.exe, resmon.exe, cleanmgr.exe
    launchers and msinfo32.exe hand-off to App Paths; services.msc,
    eventvwr.msc, devmgmt.msc, diskmgmt.msc, compmgmt.msc in system32 and
    the .msc association; the Start menu's Administrative Tools shortcuts.
  * patches/sg/0145: Win+X lists Event Viewer, Device Manager, Disk
    Management and Computer Management.
  * Gate: test/admintools-gate.sh.

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 07:16:14 -0700

wine-sg (10.0-39) unstable; urgency=medium

  * patches/sg/0143: the Event Log service (wevtsvc) keeps real logs --
    Application, System, Security and any log registered under
    Services\EventLog -- in system32\winevt\Logs, with MaxSize wraparound,
    its own start/stop/cleared events and a message table. Who may read,
    write, clear or back up a log is decided from the caller's token:
    only Administrators and SYSTEM read the Security log, only SYSTEM
    writes it.
  * patches/sg/0144: advapi32's event log functions (OpenEventLog,
    RegisterEventSource, ReportEvent, ReadEventLog, backups, clearing,
    counts, NotifyChangeEventLog) are the service's client instead of
    stubs. advapi32:eventlog 506 tests, 0 failures.
  * Gate: test/eventlog-gate.sh (make test-eventlog).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 07:06:06 -0700

wine-sg (10.0-38) unstable; urgency=medium

  * patches/sg/0140: a named pipe server impersonating its client gets the
    client's token, not its own -- in a shared prefix every service (SYSTEM)
    saw every standard user as SYSTEM. Groundwork for the SCM's and the
    event log's access checks.
  * Gate: test/pipeimp-gate.sh (make test-pipeimp).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 03:17:55 -0700

wine-sg (10.0-37) unstable; urgency=medium

  * patches/sg/0083: a pipe end reports its WriteQuotaAvailable (it was
    always 0). Programs in Git Bash's terminal could never print: the
    Cygwin/MSYS runtime waits for room before every pty write. With
    0081-0083 Git Bash reaches its prompt. Gate: make test-pipequota.

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 02:46:17 -0700

wine-sg (10.0-36) unstable; urgency=medium

  * patches/sg/0100: Notepad is a real editor -- our own editor control
    (gap buffer; line ends, encodings and BOMs kept byte for byte), tabs,
    syntax highlighting for 20 languages, find/replace bar with regular
    expressions, multi-level undo, line numbers, word wrap, zoom, status bar,
    reload on change, recent files and session, dark theme -- still
    notepad.exe in system32 and syswow64 with Windows Notepad's command line.
  * patches/sg/0101: wine.inf maps .txt/.text/.log to txtfile (ShellExecute
    of a text file failed) and opens .inf files in Notepad.
  * Gate: test/notepad-gate.sh (make test-notepad).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 02:40:00 -0700

wine-sg (10.0-35) unstable; urgency=medium

  * patches/sg/0120: calc.exe, mspaint.exe and snippingtool.exe in system32
    and syswow64, launchers for what App Paths registers (sg-shell's
    Calculator, Paint and Snipping Tool) -- CreateProcess never reads App
    Paths, so programs and scripts that run them by name found nothing.
  * patches/sg/0121: Wine's taskmgr.exe and wmplayer.exe hand off to the App
    Paths Task Manager and Media Player (as control.exe does, 0072).
  * patches/sg/0122: Win+Shift+S and Print Screen run snippingtool.exe /clip.
  * patches/sg/0123: Ctrl+Shift+Esc opens Task Manager.
  * patches/sg/0124: charmap.exe, the same launcher for the Character Map.
  * patches/sg/0125: wineboot skips startup items disabled in Task
    Manager (Explorer\StartupApproved). Gate: test/startup-gate.sh.
  * Gate: test/handoff-gate.sh (make test-handoff).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 01:53:22 -0700

wine-sg (10.0-34) unstable; urgency=medium

  * patches/sg/0082 narrowed: only pipes, events, mutexes, semaphores,
    timers and sections created without a descriptor get their creator as
    owner and group. 10.0-33 applied it to every named object, overriding
    registry keys', window stations' and desktops' own defaults, which broke
    the session's display links (taskbar laid out for 1024x768 in the VM
    boot gate). The objowner gate now checks a registry key keeps its DACL.
  * test/compat: optional registry values set before installing (Firefox's
    DisableAppUpdate policy keeps it on the pinned version).

 -- Stained Glass OS <ke7oxh@gmail.com>  Fri, 25 Sep 2026 01:42:14 -0700

wine-sg (10.0-33) unstable; urgency=medium

  * patches/sg/0081: a stack grows into read-write pages, whatever its
    reservation's protection (Cygwin/MSYS stacks; Git Bash's mintty was
    killed by SIGSEGV). Gate: make test-stackgrow.
  * patches/sg/0082: an object created without a security descriptor has its
    creator as owner and group (no DACL, access unchanged); programs in Git
    Bash's terminal segfaulted on the NULL owner. Gate: make test-objowner.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 23:38:07 -0700

wine-sg (10.0-32) unstable; urgency=medium

  * patches/sg/0094: include/Makefile.in lists wine/sgnetctl.h -- a clean
    build could not generate its Makefiles since 0079 (found by the
    compatibility work; CI red since 10.0-26).

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 22:52:26 -0700

wine-sg (10.0-31) unstable; urgency=medium

  * patches/sg/0093: ipconfig reports a failed adapter query.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 22:06:23 -0700

wine-sg (10.0-30) unstable; urgency=medium

  * patches/sg/0092: Win+H starts voice typing (sg-dictate.exe /toggle).

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 21:56:16 -0700

wine-sg (10.0-29) unstable; urgency=medium

  * patches/sg/0091: no taskbar button for a tool window (the Start menu
    showed as one).

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 21:32:42 -0700

wine-sg (10.0-28) unstable; urgency=medium

  * patches/sg/0079: ipconfig and netsh work for an account that has not
    signed in (its %TEMP% is made, or /tmp used), and a failed adapter
    lookup reports its real error. Found by sg-image's net gate.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 21:19:46 -0700

wine-sg (10.0-27) unstable; urgency=medium

  * patches/sg/0090: the shell's desktop icons (This PC, the user's files,
    Network, Recycle Bin, Control Panel) by HideDesktopIcons, in columns,
    shadowed titles; explorer.exe ::{CLSID} opens the folder. Gate: make
    test-desktop-icons.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 21:05:44 -0700

wine-sg (10.0-26) unstable; urgency=medium

  * patches/sg/0078: a native program gets the files it is given as stdin,
    stdout and stderr, also from a program with no console.
  * patches/sg/0079: ipconfig /release /renew /flushdns and netsh's
    interface ip and wlan commands, through sg-netctl. Gate: make
    test-netsh.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 20:54:05 -0700

wine-sg (10.0-25) unstable; urgency=medium

  * patches/sg/0080: WinSta0\Default is the user's desktop -- programs that
    name it for a child (LibreOffice's first start, Mozilla-style launchers)
    land on the session's desktop and can make windows. Gate: make
    test-default-desktop.
  * The application compatibility suite: make test-compat (test/compat/).

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 20:53:52 -0700

wine-sg (10.0-24) unstable; urgency=medium

  * patches/sg/0076: PrintWindow of another process's window (WM_PRINT, and
    PW_RENDERFULLCONTENT from its surface, even cloaked). Gate: make
    test-printwindow.
  * patches/sg/0077: Task View and Alt+Tab picture every window with it.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 19:58:14 -0700

wine-sg (10.0-23) unstable; urgency=medium

  * patches/sg/0073: win32u sends the move/size WinEvents.
  * patches/sg/0074: SPI_SETWORKAREA reaches every program and outlives
    display changes; the taskbar reserves its space (maximized windows stop
    above it).
  * patches/sg/0075: drag a window to the screen's edge to snap it (half,
    or maximize at the top), with a preview.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 19:35:37 -0700

wine-sg (10.0-22) unstable; urgency=medium

  * patches/sg/0072: control.exe run directly hands off to the Control
    Panel App Paths names (sg-shell's). Gate: make test-control.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 19:19:12 -0700

wine-sg (10.0-21) unstable; urgency=medium

  * patches/sg/0071: the Windows key's shortcuts -- the key alone opens
    Start; Win+D/M/Shift+M, Win+Left/Right snap, Win+Up/Down, Win+E, Win+R,
    Win+I, Win+Pause, Win+X. Gate: make test-shellkeys.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 19:14:29 -0700

wine-sg (10.0-20) unstable; urgency=medium

  * patches/sg/0070: a standard user's shell folders resolve -- the profile
    list is read read-only, and an unexpanded path is never cached (an
    empty Shell Folders\Desktop broke shortcuts and desktop icons). Gate:
    make test-shellfolders.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 19:09:22 -0700

wine-sg (10.0-19) unstable; urgency=medium

  * patches/sg/0069: the wallpaper in any picture format (WIC) and in
    Windows' styles (Fill, Fit, Stretch, Span, Center, Tile); windows and
    the taskbar are no longer painted over when it changes. Gate: make
    test-wallpaper.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 19:05:52 -0700

wine-sg (10.0-18) unstable; urgency=medium

  * patches/sg/0067: cloaked windows -- DwmSetWindowAttribute(DWMWA_CLOAK),
    DWMWA_CLOAKED, and the shell's cloak (NtUserSetWindowCloak) virtual
    desktops are built on. Changes the server protocol.
  * Gate: make test-cloak.
  * patches/sg/0068: virtual desktops and Task View in explorer
    (Win+Ctrl+D/F4/Left/Right, Win+Ctrl+Shift+Left/Right, Win+Tab, the Task
    View button), Alt+Tab, state in HKCU\...\Explorer\VirtualDesktops,
    and shell32's IVirtualDesktopManager. Gate: make test-vdesk.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 17:48:58 -0700

wine-sg (10.0-17) unstable; urgency=medium

  * patches/sg/0064: the Windows font smoothing setting decides (ClearType,
    standard, off), not the host's fontconfig.
  * patches/sg/0065: Stained Glass scroll bars -- flat track, inset flat
    thumb, solid arrows, no gripper (generated by theme/scrollbar.py).
  * patches/sg/0066: the Light theme INI's own accent colours are purple.
  * Gate: make test-theme (test/theme-gate.sh, test/theme-gallery.c).

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 16:41:23 -0700

wine-sg (10.0-16) unstable; urgency=medium

  * patches/sg/0060: MSIX/AppX deployment -- PackageManager AddPackageAsync
    (trusted signature, block map checked, extracted to WindowsApps),
    RegisterPackageAsync (development mode), RemovePackageAsync, Package
    objects with InstalledLocation and app list entries, Start menu
    shortcuts. Gate: make test-appx.
  * patches/sg/0061: the package identity of the current process
    (GetCurrentPackageFullName and friends).
  * patches/sg/0062: ApplicationData.Current is one per process.
  * patches/sg/0063: MSIX bundles -- the bundle reader, bundle signatures
    (the AppX bundle SIP), and deploying a bundle's package for this machine.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 14:07:01 -0700

wine-sg (10.0-15) unstable; urgency=medium

  * patches/sg/0056: a user's network drives and UNC paths -- drive letters
    from /run/stained-glass-net/drives/<uid> before the prefix's (a logon
    session's own DosDevices), \\server\share resolved under
    /run/stained-glass-net/unc and mounted on first use by sg-netmountd with
    the user's own Kerberos ticket; GetLogicalDrives reports them.
  * patches/sg/0057: ntlanman.dll, the Windows network provider:
    WNetAddConnection2/3, WNetCancelConnection2, WNetGetConnection; NET USE
    device: \\computer\share and NET USE device: /DELETE.
  * patches/sg/0058: \\server\share\\file is \\server\share\file (cmd
    builds that name for any script at a share root: domain logon scripts
    failed to start).
  * patches/sg/0059: USERNAME for every user of a shared prefix; wineboot
    keeps a domain logon's USERDOMAIN, LOGONSERVER and home share.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 12:17:07 -0700

wine-sg (10.0-14) unstable; urgency=medium

  * patches/sg/0055: Chromium's sandbox -- restricted tokens (restricting
    SIDs, write-restricted, deny-only user, DISABLE_MAX_PRIVILEGE),
    integrity levels, AppContainer (lowbox) tokens (NtCreateLowBoxToken,
    CreateAppContainerToken, PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES),
    TokenSecurityAttributes, and the documented proc-thread attributes.
    Microsoft Edge now runs with its sandbox on. Gate: make test-edge
    (test/token-probe.c).

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 12:02:11 -0700

wine-sg (10.0-13) unstable; urgency=medium

  * patches/sg/0048: display-config keys stay user-writable when SYSTEM
    re-stamps them (the login screen crashed at boot on a machine with
    Microsoft Edge installed).
  * patches/sg/0049-0054: what Microsoft Edge needed -- WofSetFileDataLocation,
    IsWindowArranged, GetPointerDevice, GetPointerPenInfo, PowerReadACValue,
    effective power mode notifications, InternetGetCookieEx2,
    InternetFreeCookies, DeriveAppContainerSidFromAppContainerName,
    AddConditionalAce. Edge installs and runs (without its sandbox, for now).
    Gate: make test-edge.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 10:23:42 -0700

wine-sg (10.0-12) unstable; urgency=medium

  * patches/sg/0047: a shared prefix also admits a peer whose own
    supplementary groups (SO_PEERGROUPS) include the prefix's group, as the
    kernel already lets it open the prefix's files (pam_group, sg/newgrp,
    SupplementaryGroups=).

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 07:37:48 -0700

wine-sg (10.0-11) unstable; urgency=medium

  * win32u: REG_OPTION_OPEN_LINK is the OBJ_OPENLINK attribute (0046). Display
    source links were never retargeted, so a virtual desktop could read back
    a 1024x768 primary screen and lay the taskbar out for it.

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 06:11:12 -0700

wine-sg (10.0-10) unstable; urgency=medium

  * Working winget: search, show, install (MSI and NSIS), list and uninstall
    run end to end.
  * PackageCatalog with weak references (0043); PackageManager queries answer
    (0044); the .msi verbs pass ShellExecute parameters to msiexec (0045).

 -- Stained Glass OS <ke7oxh@gmail.com>  Thu, 24 Sep 2026 00:44:55 -0700

wine-sg (10.0-9) unstable; urgency=medium

  * AppX/MSIX: appxpackaging.dll package reader with block-map enforcement
    (0035), package name functions in kernelbase (0034), the AppX signature
    SIP in wintrust (0037). winget's community source now opens.
  * Compression API (MSZIP) in cabinet.dll (0036): winget show works.
  * Security: Authenticode no longer skips the chain for SHA-256 signers
    (0039); a partial chain fails the base policy (0040).
  * crypt32: application policies extension (0041); administrator-added
    root CAs survive the host root sync (0042).
  * wintrust opens verified files with FILE_SHARE_DELETE (0038).

 -- Stained Glass OS <ke7oxh@gmail.com>  Wed, 23 Sep 2026 23:54:35 -0700

wine-sg (10.0-8) unstable; urgency=medium

  * Windows.ApplicationModel.Resources.ResourceLoader over a new PRI
    (resources.pri) reader (patch 0032): MRT-localised programs show their
    text instead of resource keys -- winget's help, tables and messages.
  * Windows.Foundation.Uri parsed with urlmon's IUri (patch 0033): host,
    path, query, port, CombineUri and the rest instead of E_NOTIMPL.

 -- Stained Glass OS <ke7oxh@gmail.com>  Wed, 23 Sep 2026 23:02:53 -0700

wine-sg (10.0-7) unstable; urgency=medium

  * Recolour the Light visual style to Stained Glass purple (patch 0031): the
    themed common controls, selection, focus and window buttons carry the
    project's purple instead of Windows-10 blue. The theme's images are
    regenerated from the recoloured SVGs by build.sh (needs rsvg-convert,
    icotool, ImageMagick -- added to make deps and Build-Depends).

 -- Stained Glass OS <dev@stained-glass.example>  Wed, 23 Sep 2026 21:03:34 -0700

wine-sg (10.0-6) unstable; urgency=medium

  * iertutil: implement Windows.Foundation.Uri's IUriEscapeStatics
    (EscapeComponent/UnescapeComponent), Stained Glass patch 0030. winget uses
    it to escape a search query into the source URL. With this and the
    Windows.Web.Http transport, real winget-cli opens the Microsoft Store source
    and returns real search results (`winget search python').

 -- Stained Glass OS <dev@stained-glass.example>  Wed, 23 Sep 2026 19:34:07 -0700

wine-sg (10.0-5) unstable; urgency=medium

  * Windows.Web.Http: response headers (Stained Glass patch 0029). winget reads
    and iterates a response's headers after each request; add the response
    header collection (typed IHttpResponseHeaderCollection + IMap<HSTRING,HSTRING>
    + IIterable), built from winhttp's raw header block. winget now reads the
    status and every header of its package source over real HTTPS.

 -- Stained Glass OS <dev@stained-glass.example>  Wed, 23 Sep 2026 17:54:54 -0700

wine-sg (10.0-4) unstable; urgency=medium

  * Implement Windows.Web.Http in windows.web.dll (Stained Glass patch 0028):
    the WinRT HTTP stack winget uses to reach a package source -- filter,
    HttpClient, HttpMethod, HttpRequestMessage, HttpStringContent, the typed
    header collections, HttpResponseMessage and a pre-completed async operation,
    with the transport driven by winhttp. Real winget-cli now sends real HTTPS
    requests to cdn.winget.microsoft.com and reads the responses.

 -- Stained Glass OS <dev@stained-glass.example>  Wed, 23 Sep 2026 17:45:43 -0700

wine-sg (10.0-3) unstable; urgency=medium

  * Run real Microsoft winget-cli under Wine (Stained Glass patches 0026-0027):
    - SHGetKnownFolderPath accepts KF_FLAG_NO_APPCONTAINER_REDIRECTION, which
      every packaged Microsoft app passes; rejecting it made winget throw
      through a noexcept boundary and die before printing (0026).
    - implement and register Windows.ApplicationModel.Core.CoreApplication with
      a working CoreApplication.Properties (a small IPropertySet), which winget
      activates at start-up (0027).
    With these, `winget --version' and `winget --help' run under wine-sg.

 -- Stained Glass OS <dev@stained-glass.example>  Wed, 23 Sep 2026 15:46:38 -0700

wine-sg (10.0-2) unstable; urgency=medium

  * Multi-user hardening and features (Stained Glass patches 0014-0025):
    - clients open/create/delete/rename/reopen/chmod files with their own Unix
      rights; per-user process agent for cross-uid ptrace/signal (D14/D16/D19).
    - only SYSTEM mints administrator tokens; owners open their own processes
      (D17/D18); per-user SID names (D12); USERPROFILE ordering (D15).
    - registry: enforce a key's DACL on subkey creation (S2 clause 3); read the
      graphics driver and machine Group Policy (SHRestricted) from HKLM.
    - "Run as administrator" routes to the elevation broker.

 -- Stained Glass OS <dev@stained-glass.example>  Wed, 23 Sep 2026 14:59:53 -0700

wine-sg (10.0-1) unstable; urgency=medium

  * Initial release: Wine 10.0 built with --enable-archs=i386,x86_64.
  * Carries four fixes from Debian's wine packaging; see patches/series.
    binutils2.44.patch is required -- without it the build produces PE files
    whose import address tables land in read-only sections, and every process
    faults during module load on Debian trixie.

 -- Stained Glass OS <ke7oxh@gmail.com>  Sun, 21 Sep 2026 00:00:00 +0000
